*.iml
.gradle
.kotlin
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Environment / secrets — never commit real values.
.env
.env.local
.env.*.local
dev.vars
.dev.vars
.wrangler/

# Signing material
*.jks
*.keystore
keystore.properties
my-upload-key.jks
debug.keystore

# Cloudflare backend build output and local state
cloudflare/dist/
cloudflare/node_modules/
cloudflare/.wrangler/
cloudflare/coverage/

# Firebase configuration (removed as part of the Cloudflare migration).
# Keep ignored so a stale file is never committed again.
google-services.json
app/google-services.json
